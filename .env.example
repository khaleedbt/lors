# Generate: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=https://lorssy.com,https://www.lorssy.com

DB_NAME=lors
DB_USER=lors_user
DB_PASSWORD=changeme
DB_HOST=localhost
DB_PORT=5432

TELEGRAM_BOT_TOKEN=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
DEEPSEEK_API_KEY=

ASSISTANT_API_KEY=
BACKEND_BASE_URL=http://127.0.0.1:8000

# Events Manager → датасет → Settings → Conversions API → Generate access token.
# META_TEST_EVENT_CODE — только на время теста (Events Manager → Test Events),
# в проде пусто/не задан.
META_DATASET_ID=
META_CAPI_TOKEN=
META_TEST_EVENT_CODE=

# One trusted nginx proxy; set 0 for direct local requests.
NUM_PROXIES=1
